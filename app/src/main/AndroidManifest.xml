<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.NFC" />

    <uses-feature android:name="android.hardware.nfc.hce" android:required="true" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/Theme.WirelessTNTN">

        <!--
            LSPosed / Xposed module declarations.

            xposedmodule   — marks this APK as an Xposed module
            xposeddescription — human-readable description shown in the LSPosed UI
            xposedminversion  — minimum XposedBridge API version required (82 = Art-based)
            xposedscope       — which processes this module hooks (com.android.nfc)
        -->
        <meta-data android:name="xposedmodule"      android:value="true" />
        <meta-data android:name="xposeddescription" android:value="Hijacks NFC LMRT to route all AIDs to PassthroughHceService" />
        <meta-data android:name="xposedminversion"  android:value="82" />
        <meta-data android:name="xposedscope"       android:resource="@array/xposed_scope" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            HCE service declaration — NO apduservice.xml meta-data.

            RegisteredServicesCache normally requires an apduservice.xml to register
            an HCE service.  Without it the framework throws XmlPullParserException
            and silently skips the service (see RegisteredServicesCache.getInstalledServices).
            LmrtHijackModule.InjectOurServiceHook compensates by injecting a synthetic
            ApduServiceInfo for this service into getInstalledServices() at runtime, so
            the service still appears in RegisteredAidCache.mUserApduServiceInfo.

            The intent filter is kept so PackageManager can find the service for the
            synthetic injection (and so the system can bind to it via HostApduService
            SERVICE_INTERFACE when an AID SELECT is dispatched by HostEmulationManager).
        -->
        <service
            android:name=".PassthroughHceService"
            android:exported="true"
            android:permission="android.permission.BIND_NFC_SERVICE">
            <intent-filter>
                <action android:name="android.nfc.cardemulation.action.HOST_APDU_SERVICE" />
            </intent-filter>
            <!-- No <meta-data> pointing to apduservice.xml — handled by Xposed hook -->
        </service>
    </application>
</manifest>
